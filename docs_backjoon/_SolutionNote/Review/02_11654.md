### 기본정보

| TITLE      | VALUE                                         |
| ---------- | --------------------------------------------- |
| **일시**     | 2026.01.22                                    |
| **번호**     | 11654                                         |
| **아이디어**   | 문자 1개를 입력받아 ASCII(정수) 코드 값으로 변환해 출력           |
| **시간복잡도**  | O(1)                                          |
| **사용 API** | `static_cast<int>()`, `std::cin`, `std::cout` |
| **경과시간**   | 1분 46초                                        |
| **결과**     | PASS                                          |
| 재풀이여부      | X                                             |

---
### 메모

- `char`는 내부적으로 정수 코드(ASCII)로 표현되므로, 정수형으로 캐스팅하면 코드값을 얻을 수 있음
    
- C++에서는 `(int)c`도 가능하지만, 명시적으로는 `static_cast<int>(c)`가 권장됨
    
- 출력에서 `std::endl`은 flush가 발생하므로 일반적으로 `'\n'`이 더 가벼움
---
### GPT 피드백

- 현재 풀이가 문제 요구사항을 정확히 충족합니다.
    
- 개선한다면 **C 스타일 캐스팅 → `static_cast<int>`**, **`std::endl` → `'\n'`**로 바꾸는 정도가 가장 깔끔합니다.

---