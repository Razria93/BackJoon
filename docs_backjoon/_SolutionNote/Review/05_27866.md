### 기본정보

| TITLE      | VALUE                                                       |
| ---------- | ----------------------------------------------------------- |
| **일시**     | 2026.01.22                                                  |
| **번호**     | 27866                                                       |
| **아이디어**   | 문자열 `S`를 입력받고, 정수 `N`(1-based)을 `N-1`로 보정하여 `S[N-1]` 문자를 출력 |
| **시간복잡도**  | 출력(인덱스 접근) O(1) / 입력 포함 전체 O(                               |
| **사용 API** | `std::string`, `std::cin`, `std::cout`, `operator[]`        |
| **경과시간**   | 2분 24초                                                      |
| **결과**     | PASS                                                        |
| 재풀이여부      | X                                                           |

---
### 메모

- `std::cin >> S`는 공백 전까지만 읽으며, 입력 문자를 **그대로 보존**하므로 대/소문자를 자동 변환하지 않음

---
### GPT 피드백

- 풀이가 정석이며 요구사항을 정확히 만족합니다.
    
- 가장 중요한 실수 포인트인 **1-based → 0-based 변환(`S[N-1]`)** 을 정확히 처리했습니다.
    
- 시간복잡도 표기도 타당합니다: 접근/출력은 O(1)이지만 문자열 입력은 길이 |S|에 비례합니다.
    
- (선택) 실전 코드에서 안전성을 더 챙기려면 `1 <= N && N <= S.size()` 범위 체크를 넣을 수 있으나, 본 문제는 입력 조건이 보장되는 전제라 필수는 아닙니다.

---