### 기본정보

| TITLE      | VALUE                                                                |
| ---------- | -------------------------------------------------------------------- |
| **일시**     | 2026.01.22                                                           |
| **번호**     | 10809                                                                |
| **아이디어**   | 알파벳 `'a'`부터 `'z'`까지 순회하며 <br>각 알파벳에 대해 문자열 `S`를 선형 탐색하여 최초 등장 위치를 찾음 |
| **시간복잡도**  | O(N) (알파벳 개수는 상수이므로 정확히는 O(26N))                                     |
| **사용 API** | `std::string`, `std::cin`, `std::cout`                               |
| **경과시간**   | 6분 1초                                                                |
| **결과**     | PASS                                                                 |
| 재풀이여부      | O                                                                    |

---
### 메모

- 알파벳 개수가 고정(26)이므로 중첩 반복문 구조이지만 전체 시간복잡도는 O(N)에 해당함
    
- 첫 등장 위치만 필요하므로 발견 즉시 `break` 처리하여 불필요한 순회를 방지함

---
### GPT 피드백

- 문제 요구사항을 정확히 만족하는 안정적인 구현입니다.
    
- 알파벳 개수가 상수이므로 전체 시간복잡도는 O(N)으로 평가됩니다.
    
- 구현이 직관적이며 코딩 테스트 환경에서 실수 가능성이 낮은 방식입니다.
    
- 문자열을 1회 순회하며 위치 배열을 갱신하는 방식도 보다 일반적인 대안으로 고려할 수 있습니다.

---