### 기본정보

| TITLE      | VALUE                                                        |
| ---------- | ------------------------------------------------------------ |
| **일시**     | 2026.01.14                                                   |
| **번호**     | 10818                                                        |
| **아이디어**   | N개 입력을 1회 순회하면서 min/max 동시 갱신<br>(초기값 `INT_MAX` / `INT_MIN`) |
| **시간복잡도**  | O(N)                                                         |
| **사용 API** | `INT_MAX`, `INT_MIN` (`<climits>`)                           |
| **경과시간**   | 5분 이내                                                        |
| **결과**     | PASS<br>(컴파일에러: 1회)                                          |
| 재풀이여부      | X                                                            |

---
### 메모

- 백준의 경우 컴파일 에러가 발생하면 에러메세지를 띄워줌. 확인해볼 것.

---
### GPT 피드백

- 개선 필요(습관)
  변수명 min, max → minValue, maxValue (이름 충돌 방지)
  
- 개선(가독성)
  루프 for (int i = 0; i < N; ++i) 형태 권장(오프바이원 방지)
  
- 개선(불필요 연산)
  비교 >=/<= → >/<로 충분
  
- 정석 참고
  입력을 벡터에 저장하는 경우 std::minmax_element 사용 가능
  (단, 본 문제는 현재 방식이 더 효율적)

---