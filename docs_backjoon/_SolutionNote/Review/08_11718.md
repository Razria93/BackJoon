### 기본정보

| TITLE      | VALUE                                                  |
| ---------- | ------------------------------------------------------ |
| **일시**     | 2026.01.29                                             |
| **번호**     | 11718                                                  |
| **아이디어**   | EOF까지 `getline`으로 한 줄씩 입력을 받아 즉시 출력                    |
| **시간복잡도**  | O(N) (입력된 전체 문자 수 기준)                                  |
| **사용 API** | `std::getline`, `std::string`, `std::cin`, `std::cout` |
| **경과시간**   | 약 33분                                                  |
| **결과**     | FAIL                                                   |
| 재풀이여부      | O                                                      |

---
### 메모

- 입력 줄 수가 명시되지 않은 문제로, EOF 기반 입력 처리가 핵심
    
- `cin >>` 대신 `getline`을 사용해야 공백 포함 문자열을 정확히 처리 가능
    
- `cout.tie()`는 의미 없으므로 사용하지 않음

---

### GPT 피드백

- 문제 의도를 정확히 반영하려면 **입력 횟수 고정 방식은 부적절**
    
- `while (getline(cin, line))` 패턴은 EOF 처리 문제의 정형 구조
    
- 불필요한 변수 분리 및 출력 스트림 tie 제거로 코드 간결화 가능
    
- BOJ 11718 / 11719 유형은 **입력 종료 조건 파악 능력**을 평가하는 문제

---