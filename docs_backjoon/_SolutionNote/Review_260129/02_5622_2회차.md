### 기본정보

| TITLE      | VALUE                                        |
| ---------- | -------------------------------------------- |
| **일시**     | 2026.01.26                                   |
| **번호**     | 5622                                         |
| **아이디어**   | 알파벳 문자(`A`–`Z`)를 인덱스로 변환하여 다이얼 소요 시간을 배열로 매핑 |
| **시간복잡도**  | O(N) — 입력 문자열 길이 N에 대해 1회 순회                 |
| **사용 API** | `std::string`, `std::cin`, `std::cout`       |
| **경과시간**   | 5분 이내                                        |
| **결과**     | PASS                                         |
| 재풀이여부      | O                                            |

---
### 메모

- 다이얼 규칙을 **조건문(if/else)** 이 아닌 **정적 배열 사용**으로 단순화
- `'A'` 기준으로 문자 인덱스를 계산하여 바로 접근하는 방식

---
### GPT 피드백

- **접근 방식은 매우 적절**합니다. 다이얼 문제의 정석적인 풀이 중 하나입니다.
    
- 시간 복잡도 O(N)으로 최적이며, 조건 분기 없이 배열 조회만 수행하는 점이 깔끔합니다.
    
- `std::cout.tie(0);` 는 실질적으로 의미가 거의 없으므로 생략해도 무방합니다.  
    (`cin` ↔ `cout` 동기화 해제는 `sync_with_stdio(false)`와 `cin.tie(nullptr)`만으로 충분)

---